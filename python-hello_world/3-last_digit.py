import random
number = random.randint(-10000, 10000)
last_digit = 0

if last_digit of number > 5
    print("last_digit number > 5 == 0")
else: 

if last_digit number == 0
print("last_digit number == 0")

if last_digit number < 6 and not 0
print("last_digit number")
